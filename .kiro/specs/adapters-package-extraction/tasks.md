# Implementation Plan: adapters-package-extraction

> ★ **两处次序不可调换**：
> ① 守卫（任务 2）必须**先于**搬迁（任务 3）扩到四包 —— 源码直连 + 跨包导入使「搬错包」在类型层完全可能通过。
> ② 主入口收窄（任务 5）必须**后于**搬迁与接线 —— 它要基于**搬完后的真实导出面**重生成基准；
> 提前做只会得到一份中间态基准。

---

- [ ] 1. 基础：新包骨架与解析登记

- [x] 1.1 建立 adapters 包的清单与依赖面
  - 新建包目录与清单：ESM、源码直连（无构建步骤）、与既有包同形的通配子路径导出
  - 三个重依赖（云沙箱 SDK / 数据库驱动 / MCP SDK）**归此包**；另需内核 / 协议 / 日志 / 校验库
  - agent 运行时 SDK 两包声明为 **peer 且不标可选**（集合内有 7 处值导入）
  - ★ 通配导出**不可省** —— 它是依赖方向守卫看得见跨包深路径边的前提，漏了守卫会**永远绿**
  - 可观测完成态：工作区可见新包，且其清单里三个重依赖均在、agent SDK 在 peer 且无 optional 标记
  - _Requirements: 1.2, 1.3, 1.5_

- [x] 1.2 建立新包的类型检查与分档测试基建
  - 以既有包的同名文件为样板建类型配置、测试配置、分档 workspace 与分档编排脚本
  - `tsconfig` 的 `rootDir` 取上一层（跨包引用内核源码否则触发 TS6059）
  - ★ 测试配置文件**必须存在**，否则静默继承仓库根配置，症状是「No test files found」
  - ★ 分档 workspace 的 setup 共享件跨包引用内核包对应文件；根级 setupFiles 在存在 workspace 文件时被**完全忽略**
  - ⚠ 测试搬入（3.2）前 fast 档必然为空、按设计就该失败 —— 本任务完成判据**不含** fast 档转绿
  - 可观测完成态：新包 `typecheck` exit 0；分档脚本可调起并明确报出「fast 档为空」
  - _Requirements: 6.4_

- [x] 1.3 在全仓解析配置中登记新包
  - 仓库根测试配置加新包别名；★ 裸包名须用 **`$` 锚定的正则**且排在前缀条**之前**
    （字符串 alias 是朴素前缀匹配，带尾斜杠那条会把裸 specifier 一起吃掉）
  - 仓库根 fast / e2e 脚本串上新包，★ **排在末尾** —— 它在 3.2 前必然失败，排中间会短路掉后面的包
  - 确认服务端构建的别名表**不加**新包（走通配导出由解析器原生展开）
  - ★ **不要加裸包名 alias** —— 新包 `exports` 只有通配、没有 `"."` 主入口（任务 1.1 定的，与 File Structure Plan 一致）。
    加了会指向不存在的文件；上一轮教训：留着指向已搬走文件的 alias 比没有 alias 更难排查
  - 可观测完成态：仓库根测试面与类型检查均不因新包而失败（★ 根 `-r` 须排除 desktop，其为既有红）
  - _Requirements: 6.4, 1.5_

- [ ] 2. 守卫先行：四包全覆盖（★ 必须早于任务 3）

- [x] 2.1 把新包纳入包根名册
  - 名册加第四项；三个维度初始声明为「尚未填充」（语义是**必须恰好为空**）
  - ★ 自证：把新包根路径改错 → 守卫必须报红。**没装上的守卫报出的绿，和真的没有违规长得一模一样**
  - 可观测完成态：名册相关用例全绿，且上述自证实验确实报红
  - _Requirements: 5.1, 5.2, 5.5_

- [x] 2.2 (P) 把层→包映射改指新包
  - adapters 层的物理归宿从兼容层改为新包；★ 过渡期用「暂存包根」标记，
    且只允许在目标包根仍声明「源模块尚未填充」时存在 —— 搬迁落地即过期报红，判据自动恢复严格
  - 保住防重言性质：判据两端仍是两个独立事实源（名册是人写的层声明，磁盘是实际归位）
  - 可观测完成态：把一个 adapters 层模块的名册归属改成内核层 → 断言报红并指出该模块
  - _Requirements: 5.3, 1.4_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/module-roster.test.ts`_

- [x] 2.3 (P) 把新包纳入通配解析与反向边检测
  - 通配包根集合纳入新包并**显式断言**它在集合内 —— 漏声明通配的后果不是导入报错而是**守卫失明**
  - 反向边检测覆盖新包：新包对兼容层的任何导入都应被判反向
  - 可观测完成态：临时在新包里加一条指向兼容层的导入 → 守卫报红并指出源与目标
  - _Requirements: 5.4, 5.1_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/dependency-guard.test.ts`_

- [x] 2.4 (P) 调整按包根的依赖规则
  - 新包规则：**允许**三个重依赖；agent SDK 仍只能是 peer
  - 兼容层规则：三个重依赖从「允许」改为**禁止**（这是 R2 的守卫化）
  - ★ 漏给新包根定规则必须**响亮失败**，不得默默按最宽松处理
  - 可观测完成态：临时把数据库驱动加进兼容层 `dependencies` → 报红并同时指出依赖名与字段名
  - _Requirements: 1.2, 2.4_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/package-deps.test.ts`_

- [ ] 3. 搬迁：12 个模块与其测试

- [x] 3.1 迁移 12 个 adapters 模块
  - 57 个实现文件整体迁入新包，**目录名保持不变**（名册按顶层模块名归类）
  - 集合**对装配层闭合**（实测无 `host-assembly` / `compat` / 主 barrel 引用），故无反向边要解；
    集合内 2 条跨模块边随包整体移动
  - 用 `git mv` 保历史，否则 diff 变成全删全增、复查无法进行
  - ★ **命名不是归属判据** —— 名字暗示属 adapters 但层归属不是的，不得随包搬走
  - ★ 搬迁会让 2.1 的「尚未填充」标记与 2.2 的「暂存包根」标记过期报红 —— 那是**设计要的**，
    按守卫提示删掉源模块相关的两个维度；测试维度**保留**到 3.2
  - ★ **无对应端口的模块不新造端口**：身份类在内核里**没有**对应端口（实测；brief 初稿假定有）。
    按普通模块随包搬迁即可 —— 造端口是逻辑变更，破「只搬不改」，且本轮无消费方要求该抽象
  - ★ 若搬迁过程中发现**必须**做的逻辑变更（不改就搬不动），**不要顺手做掉**：
    单独标注、单独成任务，使它可被独立复核。上一轮的三级解析就是这么单列的
  - 可观测完成态：新包内实现文件数与迁移前相等，新包 `typecheck` exit 0；
    身份类目录下**无新增端口文件**；若有逻辑变更则已单列成任务而非混在搬迁里
  - _Requirements: 1.1, 4.1, 4.2, 4.3, 4.4_
  - _Depends: 2.1, 2.2, 2.3, 2.4_

- [x] 3.2 迁移对应测试并保住分档归位
  - 各档测试整体迁入新包；跨包测试固件的相对路径随之修正
  - ★ **文件名后缀逐字保留**：fast 档把子进程模块别名到抛错守卫（导入不报错、**调用才报错**）
  - ★ 连同**非 `.ts` 的固件**一起搬 —— 它们只被运行时的路径字符串引用，**类型检查完全无感**
    （上一轮因此漏搬 5 个固件，另有一处 fixture 路径漏改，跑起来才现形）
  - 按守卫提示删掉测试维度的「尚未填充」标记
  - 可观测完成态：新包各档测试文件数与迁移前**逐档相等**；真实启动子进程的测试全部落在允许的档位
  - _Requirements: 6.3, 5.1_

- [x] 3.3 保障 agent SDK 在新包内可解析（★ 含一条静态断言）
  - 集合内有按字符串**逐级向上**查找 agent SDK 包目录的代码路径，其解析基准随包移动
  - ★ **仓库根有该 SDK，故漏声明在本仓恒能解析成功** —— 只在真实安装树（只装本包）失败。
    与上一轮同类陷阱同构，那次实测过：摘掉声明并删链接，解析仍成功
  - 故除「能解析出来」外，另加一条**读清单的静态断言**：agent SDK 必须声明在本包清单里
  - 可观测完成态：摘掉该声明 → 静态断言报红并说明为何要紧；还原后回绿
  - _Requirements: 1.3, 4.1_
  - _Depends: 3.1_

- [ ] 4. 装配层接线

- [x] 4.1 把装配层对 adapters 的 3 处引用改为包级 specifier
  - 装配层引用真实工厂属其职责（assembly → adapters 正向），只改引用形式不改语义
  - ★ 改错会让**默认能力面静默缺失**（不报错、能力消失），故判据取**装配测试通过**而非「没报错」
  - 可观测完成态：默认能力面装配测试通过，会话存储与 MCP 探测能力仍可取得
  - _Requirements: 2.3_
  - _Depends: 3.1_

- [ ] 5. 主入口收窄与契约留痕（★ 必须后于搬迁与接线）

- [x] 5.1 移除主入口对 adapters 模块的转发并重生成基准
  - 移除主入口的 8 条整模块转发
  - ★ 基准**有意重生成**，并产出**逐一枚举的移除清单**工件 —— 数量差不够，
    它回答不了「少的是哪几个」。**「有意移除」与「不小心弄丢」在 diff 上长得一样**
  - 旧基准另存，使「收窄前的契约」可考古
  - ⚠ 与前两轮的硬约束**相反**：那两轮禁止改基准；本轮改基准是目标，但**改法**受约束 ——
    须留痕、须可枚举、改后继续以「与新基准逐字相同」把关
  - 可观测完成态：移除清单逐一列出被移除符号；基准用例以新基准通过
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 5.2 兼容层依赖面移除与主版本号
  - 兼容层 `dependencies` 移除三个重依赖，加入新包依赖
  - 版本号体现本次为破坏性变更
  - ★ **`ws` 也要一并移除**（3.1 发现）：兼容层对它已**零引用**（唯一使用者 `sandbox-transport` 已搬走），但 2.4 给 server 定的 `forbidden` 只列了 `hono / e2b / pg / MCP SDK`，**不含 `ws`** ⇒ 守卫抓不到这条死声明。须把 `ws` 加进 server 的 `forbidden` 与 `pendingRemoval`
  - ★ 判据是**依赖闭包**而非「搜不到导入语句」。⚠ 预期闭包层仍有 MCP SDK 经工具包传递
    （既有依赖），故达成范围须表述为「兼容层**自身声明**不再含三者」，闭包残留须说明来源
  - 可观测完成态：兼容层清单不含三者且守卫报绿；闭包遍历输出逐项列出三者的去留与来源
  - _Requirements: 2.1, 2.2, 3.5_
  - _Depends: 5.1_

- [ ] 6. 验证与证据

- [ ] 6.1 四包通过面与类型检查不回退
  - 四包与仓库根测试面全量运行，**连跑两次**并比对
  - ★ 核对汇总行算术：通过数 + 跳过数是否等于总数。不相等意味着 worker 静默崩溃（会被计成「0 failed」）
  - 四包与仓库根类型检查 exit 0；★ 根 `-r` 须排除 desktop（其 `typecheck` 是 Rust 构建，既有红）
  - 可观测完成态：两次运行逐档一致且不低于开工快照（284 执行文件 / 2601 用例）；实测输出留档
  - _Requirements: 6.1, 6.2_

- [ ] 6.2 两类「类型检查看不见」的失效专项搜索
  - ★ 本仓两次栽在这上面：运行时路径字符串（fixture 路径）、重复写死的契约断言（两处字节契约）
  - 搜索运行时按字符串解析的路径与包名；搜索同一契约是否在多处各写死一份
  - ★ 已知至少一处同类待修：被实测证伪的「构建器内联模块自身 URL」注释**第三处**
    （上一轮只修了两处，同一句错话散落三处说明当年是复制传播的）
  - 可观测完成态：给出搜索命令与结果；已知的第三处注释已修正并附实测复核命令
  - _Requirements: 6.6_

- [ ] 6.3 交付证据矩阵与已知未验清单
  - 30 条验收标准逐条对应**机械证据**（命令 + 实测输出），并**分级标注证据强度**
  - ★ 主入口移除清单、依赖闭包逐项去留、装配能力三项须单列
  - 跨仓消费方登记为 Revalidation Trigger，写明它们须按新主版本适配、本仓不承担该改动
  - 三处既有红（desktop Rust 构建 / 根 app 档 worker OOM / 产物冒烟的登录门）单独成节，
    并复核形态未变 —— 混在一起等于让读的人自己分辨新账旧账
  - ★ 交付前走完成度闸门取新鲜证据 —— 断言不等于证据
  - 可观测完成态：证据矩阵覆盖 30 条，每行给命令与输出摘要；未验项与既有红各自成节
  - _Requirements: 3.6, 6.5, 2.2_
  - _Depends: 6.1, 6.2_

## Implementation Notes

- 1.1：新包 `exports` **只有通配** `"./*.js": "./src/*.ts"`，无 `"."` 主入口 —— 未规划 `src/index.ts`，兼容层按深路径消费。⇒ 1.3 不得加裸包名 alias（会指向不存在的文件）。
- 1.1：刻意**不含** `scripts` 块（脚本要指向 1.2 才建的分档编排文件）。⚠ 静默性质：`pnpm -r run typecheck` 会**跳过**无该脚本的包，而根类型检查照样 exit 0 —— 1.2 须补上。
- 1.1：`devDependencies` 加了数据库驱动的类型包；agent SDK **未**误入 devDeps（否则直接触发 peer-only 守卫）。三个重依赖的版本与兼容层现声明**逐字一致**，为 5.2 的移除做对照基准。
- 1.1：新包依赖中**没有**工具包 —— 与 research.md 的 57 文件扫描一致（该集合零引用工具包）。这与 runner 包不同（runner 必须依赖工具包，内置扩展从那里来）。
- 1.2：`rootDir` 取 `".."`（跨包引内核源码，取 `"."` 会 TS6059）；`include` 额外纳入两个 vitest 配置 —— `src/` 在 3.1 前为空，不纳入别的文件 tsc 会因「无输入」报错。与 runner 那轮同解。
- ★ 1.2：新包真的进了根类型检查，由**判别实验**证明 —— 在 `src/` 埋类型错误，根检查报 `packages/adapters typecheck: src/__probe.ts(1,14): error TS2322` 并 exit 2（**错误归属到本包**）；移除后回 exit 0。只跑一次绿分不出「检查了且没问题」与「压根没检查」。
- 1.2：fast 档为空按设计**响亮失败**（`No test files found, exiting with code 1`），同一次运行里 fast-mock 报 `code 0` —— 那道刻意的不对称保住了：fast 档装守卫，变空必须响。
- 1.2：分档 workspace 的两个 setup 共享件**跨包引内核包**（`../core/test/setup/*`），且注释记明「根级 setupFiles 在本文件存在时被完全忽略，必须写进每个 project」「setup 文件必须用绝对路径」。
- ★ 1.3：只加**一条** `.js`→`.ts` 正则 alias，**没有**裸名条也没有前缀条 —— 与 runner 那轮的 4 条不同。理由：新包无 `"."` 主入口、无 `src/index.ts`，深路径按 NodeNext 约定都带 `.js` 后缀，故一条正则足够。判别实验双向验过：深路径 `@blksails/pi-web-adapters/__probe.js` 解析成功；**裸包名 `Failed to resolve`** —— 那是**正确行为**，不是缺陷。
- 1.3：串联顺序 `core → server → runner → adapters`，新包**排末尾**。实测四包全部跑到，adapters 在末尾如期失败（fast 档为空）。上一轮实测过排中间会短路掉后面的包（`core && runner && server` 时 server 完全没跑到）。
- 1.3：根 app 档 `104 passed | 1 skipped (106)` / `1019 passed | 2 skipped (1031)` —— 与既有 heap OOM 形态**逐字相同**，无新增 `N failed`。判据取逐字比对而非「全绿」：上一轮正是这样认出新增的 `1 failed`（当时总数缺口同样是 1，极易连新账一起当老账）。
- ★ 2.1：机制是**现成的** —— `pendingContributions`（语义「必须恰好为空」）与 `assertRootsContributed` 的双向强制都是上一轮为「加第 N 个包」建的，本轮只加第四项。上一轮在此花力气解决的张力（守卫先装、包还空）直接复用。
- ★ 2.1 加第四项后立刻抓到一条**真缺口**：`package-deps.test.ts` 的「策略表必须覆盖全部包根（漏定规则即失败，不得默认放行）」报红 —— 新包还没有依赖策略。**这条守卫上一轮就建好了，本轮它按设计工作**。红属**任务 2.4**，本任务不修（混着修会让每个任务的判别力都说不清）。
- 2.1：2.2 / 2.3 此刻**无红** —— 新包 `src/` 仍为空（pending），层→包映射与通配扫描还看不到违规。2.2 的映射改动须在 3.1 搬文件**之前**完成。
- 2.1：自证实验报红正确（包根路径改错 → `expected 'adapters-TYPO' to be 'adapters'`）。全量算术双向自洽：文件 1+120+2=123、用例 2+1170+3=1175。
- ★★ 2.4 用**判据翻转**解掉那个时序张力（兼容层禁令已定，但三者要到 5.2 才移得掉）：策略项加 `pendingRemoval` 标记，语义是「此刻**必须仍然**违规」，清理干净的那一刻标记即过期报红。与 `pendingContributions`（2.1）、`stagedIn`（2.2）**同构** —— 这是同一模式的第三次应用，已可视为本仓的既有解法：**过渡期把判据翻转，而不是把守卫关掉**。父层实测：模拟 5.2 移除三者后，报「包 server 的 "e2b" 已经不在依赖声明里了 —— pendingRemoval 标记过期，请把它从 `PACKAGE_DEP_POLICIES.server.pendingRemoval` 里删掉」。⇒ 5.2 落地时守卫会**逼着**删标记，不留需人记得打开的开关。
- ★ 2.4 另分清了一件容易混的事：`PEER_ONLY_DEPS`（出现在 deps/devDeps 里算违规）与按包根的 `peerOnly`（**必须**出现在 peerDeps）是两回事。`pi-ai` 只有 adapters 声明为 peer，内核与 runner 根本不引用它 —— 混在一起会让那两个包因「没声明一个它们不用的 peer」凭空变红。
- ★ 2.3 的关键设计：新包 `src/` 到 3.1 才填充，在那之前「守卫看得见新包的跨包边」**没有任何用例在证**。解法是拿真实解析器 + 真实名册，对**将来必然出现**的 specifier 直接判一次 —— 不依赖新包有没有文件，**也不需要有人回来打开开关**。而这个包最不能失明：它整包只有通配导出、没有 `"."` 主入口，兼容层将来全部按深路径消费它。
- ★ 2.2 把自毁装置装在**真实表**上而非只在合成表上：`expectedRootOf("adapters", pending)` 判兼容层、非 pending 时判新包，过渡语义双向都测。3.1 搬进第一个模块 → pending 被逼退 → `stagedIn` 随即过期报红。
- 2.2/2.3/2.4：三条判别实验由父层独立复跑（并发子代理自报不足采信），每次均确认无残留。合并后 `1189 passed | 3 skipped (1192)`（较 2.1 后 +17），typecheck exit 0。
- ★★ 3.1 暴露我勘察方法的一个缺口：research.md 那份依赖扫描只匹配 `from "..."`，**完全漏掉动态导入**。实测 adapters 集合里 `e2b`×3 / `ws`×1 / `pg`×1 全部是 `await import(...)` —— 于是 `ws` 从未出现在我列的依赖清单里，是实施者搬迁时自己发现并补进新包的。⇒ 依赖清点必须同时扫 `from "x"` 与 `import("x")` 两种形态。
- ★ 3.1 由此发现一条交给 5.2 的确切缺口：兼容层对 `ws` 已**零引用**（唯一使用者随 `sandbox-transport` 搬走），但 2.4 给 server 定的 `forbidden` 不含 `ws` ⇒ 那条死声明**守卫抓不到**。已写进 5.2 的条目。
- ★ 3.1 为可编译性有限度越界（已在派单里授权并要求说明）：主 barrel 的 8 条 `export *` 改指**新包的包级 specifier**（导出面**逐字不变**，5.1 再整体移除）；装配层 4 处引用同样改包级 specifier（4.1 的范围）。这样每一步都可独立编译与复核，而不是让某一步处在编译不过的中间态。
- ★ 3.1 新增两个 `compat/` 薄转发（`model-options` / `vision-model-options`）：Node 的 `exports` 目标**只能指向本包内的相对路径**，无法直接指向别的包，而这两条子路径已发布上游 —— 故必须经本包转发。兼容层 `exports` 仍 **8 条，一条不少**（R2.2）。
- ⚠⚠ 3.1 教训：**搬迁期间 IDE 诊断不可信**。本任务里我被它误导两次 —— 先据诊断判「接线没做完」（实际 `src/` 零错误、186 个错误全在 `test/`，属 3.2 范围），又据诊断疑「根解析断了」（实际根 `tsc` exit 0，经 `packages/server/node_modules/` 的链接向上走解析成功）。实况只认 `tsc`。

### 3.2 归属判断与逐档对照

**按文件逐个判归属**（不是按目录整体搬）—— 9 个目录整体搬走、3 个目录一分为二：

| 目录 | → adapters | 留 server | 判据 |
|---|---|---|---|
| ai-gateway / auth / extensions / identity / llm-gateway / model-sources / rpc-channel / sandbox-image / tokens | 7/9/14/3/2/1/9/2/2 | 0 | 被测实现已全搬走 |
| attachment-bridge / config / session-store | 1/1/2 | 1/1/2 | **部分**文件测的是留在兼容层的东西 |

**逐档与开工快照对照（口径：`pnpm test` 执行数）**

| 包 | 磁盘 | e2e | 执行 |
|---|---|---|---|
| core | 174 | 0 | 174 |
| server | 13（原 66） | 0（原 3） | 13 |
| runner | 47 | 0 | 47 |
| adapters | 53 | 3 | 50 |
| **合计** | **287** | 3 | **284 = 开工快照 284** ✓ |

- ★ 3.2：**执行文件总数 284 与开工快照逐字相同**，磁盘总数 287 亦与基线一致 —— 无丢失、无重复。兼容层的 2 个 mock 档与 3 个 e2e 档整体移入新包（server 两档现为 0）。
- ★ 3.2：`packages/server/typecheck` 的 186 个错误**清零**；四包 + 整仓（排除 desktop）typecheck 全 exit 0 —— 3.1 以来首次恢复。
- ★ 3.2：`testFiles` pending 已按守卫要求删除。至此本轮四个翻转判据豁免（`srcModules` / `srcFiles` / `stagedIn` / `testFiles`）**全部如期退场**，只剩 2.4 给兼容层重依赖立的 `pendingRemoval`，它在 5.2 到期。
- 3.2：父层清理了 9 个搬空后残留的空目录（git 不跟踪空目录，但工作树里是垃圾）。
- ★★ 3.3 **假绿在本轮实测复现**（不是照抄上一轮结论）：摘掉新包 `peerDependencies` 里的 agent SDK 后，`1 failed | 4 passed` —— 只有新增的**静态断言**报红，那条运行断言（`resolvePiCliEntry()` 返回真实存在的路径）**仍然通过**，因为 `locatePackageDir` 按字符串逐级向上走、命中了仓库根。⇒「能解析出来」在 monorepo 里恒真，守不住依赖声明。
- ★ 3.3 断言针对 `peerDependencies` 而非 `dependencies` —— 本包把 SDK 两包都声明为 peer（1.1 定的）。照抄上一轮 runner 那条（查 `dependencies`）会写出一条**永远为真**的断言。
- ★ 3.3 报红消息把要害写清了：「解析器只按字符串向上找 `node_modules`、从不读清单，故本仓漏声明也照样解析成功；但沙箱/standalone 只装本包时会装不出 SDK，pi CLI 扩展操作（install/list/remove）整体失效」。
- 3.3 顺带修了 4 处**注释里的交叉引用**（`e2e/llm-gateway/server-entry.ts`、3 个根级 integration 测试）—— 指向已搬走的测试目录，留着会误导人。属轻微越界但方向正确。
- ★ 4.1 实际是 **12 处**引用而非我先前勘察说的 3 处 —— 当时只 grep 了 `session-store-postgres` 与 `mcp-probe` 两个模块名，漏了网关（llm-gateway / ai-gateway routes）、认证（auth-routes / shell-credential-route / egress-model-source）、身份（identity-routes / types）、扩展（extensions/routes）那批。**这是本轮我勘察偏窄的第三次**（前两次：漏动态导入 ⇒ 漏 `ws`；本次漏引用点）。教训：勘察引用面要按**目标包**整体 grep，不要按记得的模块名逐个查。
- ★ 4.1 判据取「装配测试通过」而非「没报错」，并由**判别实验**证明该测试真的在守那些引用：打断 12 处中的一处（`mcp-probe`）→ `Test Files 1 failed`；还原后 7 用例全绿。若不做这一步，就只知道「测试是绿的」，不知道「它是否覆盖了这些引用」。
- 4.1：`IdentityProvider` 确认是 **adapters 包内的本地类型**（`identity/types.js`），不是内核端口 —— 与 brief 修订时的实测一致（R4.2 因此不为它造端口）。

### 5.1 契约收窄的账（有意的破坏性变更）

| 项 | 数 |
|---|---|
| 收窄前主入口值符号（另存 `main-entry-symbols.before-adapters-extraction.txt`） | 313 |
| 收窄后 | 224 |
| **基准记录的差** | **89**（仅运行期值） |
| **移除清单枚举的真实契约损失** | **161** = 89 值 + **72 纯类型** |
| 8 模块导出并集 166 − 仍由内核导出的 5 个 | = 161 |
| 消费方改动 | **30 个文件**（`lib/app/*` / `server/cli/install/*` / 根 `test/*` / `e2e/*`） |

- ★★ 5.1 发现一处**基准的盲区**，超出预料：常驻符号基准是 `Object.keys(await jiti.import(...))` 的产物，**只看得见运行期值**。故它记录的收窄是 89 个，而真实契约损失是 **161** 个 —— 另外 **72 个纯类型在基准里从来没出现过，它们消失时基准一声不响**。跨仓消费方的 `tsc` 照样报红（本仓实测：`AiGatewayConfig` / `EgressModel` / `LlmGatewayProviderTable` 就是这样暴露的）。⇒ **只读符号基准会低估本次变更**。
- ★ 由此回看前两轮：那两轮把 313 基准当「契约的机械判据」是**有盲区**的 —— 它们没删东西所以没造成损失，基准足以证明「没变」，但**对纯类型的增删是瞎的**。这条应写进最终交付。
- ★ 5.1 的排除算得很准：5 个符号不在移除清单里，因为它们同时由内核包导出、经 `export * from core` 仍在主入口 ⇒ 166 并集 − 5 = 161。
- ★ 5.1 移除清单格式 `<原转发模块> TAB <符号名> TAB <value|type-only>`，一符号一行，并写明消费方迁移路径。父层抽查 3 个：均已不在主入口、且都能从新包对应模块导入 —— **清单可用，不是摆设**。
- 5.1 消费方改动 **30 个文件**，超过我派单时设的 20 个阈值 —— 这是本次契约变更的真实代价，如实记录而非淡化。

### 5.2 依赖闭包实测（★ R2 的真实达成范围，不容乐观）

兼容层 `dependencies` 已移除 `e2b` / `pg` / MCP SDK / `ws` 四者（外加两个 `@types`），版本 `0.6.1 → 0.7.0`
（0.x 包的破坏性跃迁：`^0.6.1` 的范围是 `>=0.6.1 <0.7.0`，不会自动升上去）。

**但传递闭包完全没变** —— 父层实测遍历：

| 重依赖 | 声明层 | 闭包层 | 来源链 |
|---|---|---|---|
| `e2b` | 已移除 ✓ | **仍在** | server → adapters |
| `pg` | 已移除 ✓ | **仍在** | server → adapters |
| `ws` | 已移除 ✓ | **仍在** | server → adapters |
| MCP SDK | 已移除 ✓ | **仍在** | server → adapters → core → tool-kit |

★★ 结论：**R2.1 在「声明层」达成，但其背后的目的（部署方不再连带装上这些）对兼容层消费方
**完全没有兑现** —— 装 `@blksails/pi-web-server` 的人照样拿到全部四个，因为它非可选地依赖 adapters
（`host-assembly` 有 12 处引用真实工厂，无法改成可选）。
这**机械证实**了开工前对用户的判断：「切出 adapters 包不会给现有消费方省下任何依赖」。
收益只对「绕开兼容层、直接用 core + 选定 adapter 组装」的宿主成立。
⇒ 6.3 交付必须这样表述，不得笼统写成「兼容层依赖面已缩小」。

- ★ 5.2 `pendingRemoval` 如期到期退场，且 `forbidden` **正向生效**（父层判别实验：把 `pg` 加回兼容层 → 报 `pg @ dependencies(数据库驱动)`，名与字段同现）—— 约束没有随标记一起消失。至此本轮五个翻转判据豁免**全部退场**。
- ★ 5.2 策略里 `kind` 从 `exempt` 翻成 `audited`，注释点明：兼容层的依赖面**上一轮是完全无人看管的**。
- 5.2 `ws` 直接进 `forbidden` 而非 `pendingRemoval`（后者语义是「此刻必须仍然违规」，对一个当场清掉的名字用它会立即自毁）—— 判断正确。
