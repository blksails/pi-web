# Implementation Plan

- [ ] 1. 基础：回归基线与共享契约原语
- [x] 1.1 采集本期改动前的四面回归基线
  - 分别运行递归子包单测、根应用测试、离线 Node 端到端、递归类型检查四个测试面，各自记录通过/失败计数
  - 运行既有浏览器 e2e 检阅并逐项记录结果
  - 观察态：五份基线快照落到 spec 目录下可复查的记录中，任何**改动前即失败**的用例被显式标注为既有问题
  - 说明：四面必须分别采集——后续会改动包主入口，而主入口是根应用与其它子包的消费面，只跑单个包会漏红
  - _Requirements: 10.2, 10.3_

- [x] 1.2 契约版本常量与装配期版本自检
  - 提供整个契约共用的版本常量与不一致即抛的自检函数
  - 版本判定为严格相等，不提供向下兼容分支
  - 错误携带双方版本号
  - 观察态：以不一致版本调用自检会抛出携带期望值与实际值的类型化错误；一致时静默返回
  - _Requirements: 9.1, 9.2, 9.3_

- [x] 1.3 宿主状态存储的类型契约与错误分类
  - 定义双根工作区与命名空间的方法签名、写入选项、JSON 对象类型
  - 定义四类错误及其稳定判别码，错误携带键名等上下文字段
  - 端口对象自带版本字段，使实现在类型层无法声明错误版本
  - **只交付类型与错误，不建该模块的出口文件**——出口由 6.1 在全部实现就位后统一收口，避免出口引用尚不存在的符号而使整段核心期的类型检查为红
  - 观察态：四类错误均可经判别码区分且各自携带上下文；类型可被其它模块按具体文件路径 import 而无需任何实现
  - _Requirements: 2.1, 2.2, 3.4, 4.1, 9.1_

- [ ] 2. 核心：纯逻辑原语
- [x] 2.1 (P) 键空间校验
  - 拒绝相对段、绝对路径、空段、尾随分隔符、反斜杠、空字符与空串
  - 接受以分隔符连接的多段相对键；键按大小写敏感处理
  - 校验在触及任何存储之前完成
  - 观察态：穷举非法形态的单测全绿，每例断言判别码为键错误且错误携带原键
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_
  - _Boundary: workspace/key，不建也不改模块出口_

- [x] 2.2 (P) 单键值上限的环境变量解析
  - 未设置时采用默认上限；设为正整数时以该值为准
  - 不可解析、非整数、非正值一律在装配期抛错，不静默回落默认
  - 解析函数接受注入的环境变量对象而非直接读取全局
  - 观察态：按「缺省/合法/各类非法」分组的单测全绿，非法各形态逐一断言抛错
  - _Requirements: 3.1, 3.2, 3.3_
  - _Boundary: workspace/limit-config，不建也不改模块出口_

- [x] 2.3 (P) 深度合并语义
  - 嵌套对象递归合并、数组整体替换、忽略未定义值
  - 覆盖模式使既有值中本次未提供的字段被删除
  - 观察态：单测断言合并结果与既有配置落盘器的行为逐项一致，含数组替换与删除语义
  - _Requirements: 2.3, 2.4_
  - _Boundary: workspace/merge，不建也不改模块出口_

- [ ] 3. 核心：一致性测试套件
- [x] 3.1 套件骨架、测试子模块出口与键空间用例组
  - 定义最小测试框架契约、验收目标类型与**可携带上限的工厂参数类型**，套件本身不 import 任何测试框架，断言使用内置断言库
  - 错误判定一律按稳定判别码，不使用构造函数判定
  - 交付测试子模块的出口文件，作为对外子路径的入口
  - 覆盖键空间全部非法与合法形态
  - 交付两个**测试夹具**内存实现（一个合规、一个故意不校验键），落测试镜像目录，**不经对外子路径导出**
  - 观察态：套件可被传入任意 describe/it 实现驱动；对违规夹具运行时键空间用例组失败，对合规夹具运行时通过
  - _Requirements: 8.1, 8.2, 8.6, 1.1, 1.2, 1.3, 1.4, 1.5, 1.6_
  - _Depends: 1.3_

- [x] 3.2 读写语义与双根隔离用例组
  - 覆盖缺失键返回空、损坏值抛错、两种合并模式、非递归且字典序的列举、幂等删除、存在性查询、读己之写
  - 覆盖同键分写两根互不覆盖、单根删除不影响另一根、单根列举不串另一根
  - 观察态：用例组逐条锚定验收编号，且对 3.1 的合规夹具全部通过
  - _Requirements: 8.2, 2.1, 2.2, 2.3, 2.4, 2.5, 2.7, 2.8, 2.9, 4.1, 4.2, 4.3, 4.4_

- [x] 3.3 上限三例与并发原子可见性用例组
  - 上限三例：写超限抛错、经工厂参数指定更大上限后新上限生效、上限调小后既有超限值仍可读
  - 上限用例经工厂参数取得不同上限的实例，**不得改写进程环境**
  - 并发用例以并发编排发起多次互不相同的整值写入与若干次读取，断言每次读回完整等于某一次写入或初始空值，不接受字段混合体
  - 并发用例不使用固定等待
  - 观察态：三例上限用例与并发用例均在套件内，且工厂被以不同上限多次调用并产出相互隔离的实例
  - _Requirements: 8.3, 8.4, 8.6, 2.6, 3.4, 3.5_

- [ ] 4. 核心：本地参照实现
- [x] 4.1 本地实现的键映射、基本读写与合并落地
  - 键映射为根目录下的相对路径，映射使用路径拼接而非字符串连接
  - 缺失键读为空对象；内容非法时抛损坏错误；其余读写失败抛 IO 错误
  - 写入按两种合并模式落地既有合并语义
  - 列举只返回直接子级中持有值的键并按字典序升序
  - 观察态：针对本地实现的直接单测覆盖读缺失、读损坏、两种合并模式、列举非递归与幂等删除
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.7, 2.8, 2.9_
  - _Depends: 2.1, 2.3_

- [x] 4.2 原子写入、落盘权限、写时上限校验与「值/分组不可同址」
  - 写入经同目录临时文件后重命名，使读取方绝不观察到部分写入
  - 目录与文件权限与既有配置落盘器一致
  - 上限只在写入路径校验，读取路径不设限
  - **新增（主控裁决，2026-07-21，任务 4.1 复核升级；契约勘误⑧）**：落地「值与分组不可同址」——写入时若键的任一严格前缀已是值键、或该键本身是某既有值键的严格前缀，抛**键非法错误**（不是 IO 错误），`reason` 说明与哪个键冲突；相应地读取一个只是分组前缀的键须返回空对象，**不得**抛 IO 错误（4.1 当前对 `readJson("g")` 抛 `EISDIR → WorkspaceIoError`，本任务须改正）。两条互为表里，同任务落地
  - 观察态：以大于当前上限的既有值验证读取仍完整返回，同时写入被拒并抛超限错误；同址两个方向各有一例断言抛键非法错误，且读分组返回空对象
  - _Requirements: 2.6, 3.4, 3.5, 1.7, 1.8_
  - _Depends: 2.2_

- [x] 4.3 双根装配与上限接线
  - 用户级根与项目级根分别装配，两者相互隔离
  - 上限缺省取自环境变量解析结果，可由构造参数覆盖
  - 观察态：以两个不同临时目录构造实例后，同键分写两根各自独立读回
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 3.1_

- [x] 4.4 令本地实现通过完整一致性套件
  - **前置修复（主控裁决，2026-07-21，任务 4.1 复核暴露）**：套件的列举排序用例把「排序口径」与「键大小写敏感」耦合进同一用例（同时写入仅大小写不同的键对），在大小写不敏感的载体上这些键塌为同一文件，使排序断言在半数宿主上**原理上无法成立**。须改用无大小写碰撞的键组验证排序；大小写敏感另立用例，并允许实现声明其为平台不适用。依据：契约 §3.2 第 4 条（勘误⑦）
  - 在既有测试目录中以本地实现为目标驱动套件
  - **前置补充二（同批裁决，契约勘误⑧）**：套件须新增「值与分组不可同址」用例组固化该规则——同址的两个写入方向各一例断言抛键非法错误，读分组前缀一例断言返回空对象。这三例对**扁平 KV 后端**同样有判别力（不加校验的 KV 实现会写成功 / 读到空以外的东西），故须一并加到 3.1 的违规夹具自证里，否则该组是恒真断言
  - 工厂接受上限参数并每次调用产出使用独立临时根的隔离目标，用例间不串数据
  - 观察态：套件全部用例对本地实现通过，且用例数与套件声明的用例组一致
  - _Requirements: 8.5, 8.6, 1.7, 1.8_
  - _Depends: 3.3, 4.3_

- [ ] 5. 核心：其余三端口
- [ ] 5.1 (P) 能力授予契约类型
  - 快照各字段独立可选；每项授予带失效时刻
  - 附件授予仅在带会话标识请求时出现
  - 结构化定义出口授予类型，不 import 引入 pi SDK 值的模块
  - 本模块自建其出口文件（单任务独占，无并行争抢）
  - 观察态：类型层结构兼容断言通过，证明出口授予类型与既有出口输入形状可互换
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7_
  - _Boundary: capability_

- [ ] 5.2 (P) 能力面名册与组装引擎
  - 冻结 v1 的能力标识名册，**恰 16 项**：15 个路由能力 + 1 个非路由的宿主命令能力。既有 17 个注入式路由工厂中，两个工具领域的模型清单端点不入名册（按设计判定为领域泄漏、将被删除而非表态），故 17 − 2 = 15
  - 组装时先校验未知标识、后校验缺失表态，两者均抛携带标识列表的错误
  - 弃用表态要求非空原因并经回调通知；沿用与替换分别采用默认与宿主工厂
  - 路由类型与依赖对象均泛型化，不依赖既有 HTTP 模块
  - 观察态：单测覆盖缺表态、未知标识、空白原因、替换生效与弃用回调五种情形，并断言名册长度恰为 16
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7_
  - _Boundary: host-manifest_

- [ ] 5.3 (P) 配置域注册表与宿主默认域
  - 注册后立即可查询与列举，列举顺序按注册顺序稳定
  - 重复标识抛错而非静默覆盖；标识须满足键空间规则且不含分隔符
  - 默认只注册宿主关切的四个域，不注册任何工具领域域
  - 观察态：单测断言默认注册集恰为四个宿主域且不含工具领域域
  - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5_
  - _Boundary: config-domain_
  - _Depends: 2.1_

- [ ] 6. 集成：模块出口与对外导出
- [ ] 6.1 宿主状态存储模块的出口收口
  - 在全部实现就位后一次性编写该模块的具名重导出出口，涵盖类型、错误、键校验、上限解析、合并与本地实现
  - 出口头部按既有约定注明模块名、对应 spec 与 pi-SDK-free 状态
  - 说明：本任务独立存在，是为避免出口在实现完成前引用不存在的符号而使核心期类型检查持续为红
  - 观察态：该模块出口可解析全部符号，且递归类型检查零错误
  - _Requirements: 10.1_
  - _Depends: 2.1, 2.2, 2.3, 4.3_

- [ ] 6.2 包主入口导出五个契约模块
  - 逐模块新增重导出：四个端口模块 **加上根级版本常量模块**
  - 每条按既有约定附无 pi SDK 值导入的论证注释；不删改任何既有导出行
  - 观察态：从包主入口可 import 五个模块的公开符号（含版本常量），且递归类型检查零错误
  - _Requirements: 9.1, 10.1, 10.4_
  - _Depends: 1.2, 6.1, 5.1, 5.2, 5.3_

- [ ] 6.3 一致性套件的子路径导出
  - 包清单新增测试套件子路径，指向 3.1 交付的测试子模块出口
  - 不改动依赖声明
  - **★ 到期决策点（主控，2026-07-21，任务 4.4 复核提出）**：本任务是「载体事实」字段形状的**最后修改窗口**——一旦经子路径导出、pi-clouds 消费，移动字段就从改两行变成跨仓破坏性变更（与 4.3 的 `envName → source` 同构）。开工时先数 `ConformanceTarget` 上的载体事实字段：**仅 `caseSensitiveKeys` 一个则保持扁平**（一个字段不值得建命名空间）；**已达两个或以上则收进 `readonly carrier?: {...}`**。按当时实际计数决定，不提前抽象也不拖过窗口
  - 观察态：经子路径可 import 套件入口符号
  - _Requirements: 8.1_
  - _Depends: 3.1_

- [ ] 7. 验证：回归与检阅
- [ ] 7.1 四面回归与类型检查
  - 分别运行递归子包单测、根应用测试、离线 Node 端到端、递归类型检查四个测试面
  - 与 1.1 的基线逐面比对，新增失败一律视为本期缺陷
  - 观察态：四面各自通过数不低于基线且无新增失败；类型检查零错误
  - _Requirements: 10.1, 10.2_
  - _Depends: 6.2, 6.3_

- [ ] 7.2 浏览器 e2e 检阅
  - 运行既有浏览器端到端检阅并与基线逐项比对
  - 基线中已失败的用例保持既有问题标注，不混入本期结论
  - 观察态：e2e 结果与基线逐项一致，无本期引入的新增失败
  - _Requirements: 10.3_
  - _Depends: 7.1_

## Implementation Notes

- **任务 4.1（2026-07-21）**：`readAt` 曾只对 `ENOENT` 返回 `{}`，而 `list`/`exists` 都把 `ENOTDIR` 视为「不存在」——同一实例对同一个键给出互斥答案，违反 Req 2.1。复核抓出并正向对齐。**教训**：涉及「不存在」判定的方法必须共享**唯一**口径，逐方法各写各的 errno 分支必然漂移。
- **任务 4.1（2026-07-21）**：packages/server 的实测基线是 **234 文件面 / 1939 用例**（不是先前记的 104）。后续任务按此比对。
- **任务 4.1 复核升级 → 契约勘误⑦⑧**：两条跨实现分歧由**契约**收口而非由实现各自决定——
  ⑦ 大小写敏感是键空间语义，大小写不敏感载体上**保证不了**，实现须声明限制、套件不得用仅大小写不同的键对验证其它维度；
  ⑧ 值与分组不可同址（Req 1.7/1.8），落地在 4.2、套件固化在 4.4。
  **教训**：实现层遇到「载体做不到」时，正确动作是上报契约，不是在实现里悄悄定一个只有自己遵守的规矩。
- **平台红线**：Windows 设备名 / NTFS ADS 在键空间层面**合法**，能否落盘由各实现声明；**跨平台真机验证前，任何文档与报告都不得宣称 Windows 可用**。
- **任务 4.2（2026-07-21）**：反向同址校验一度把「路径在载体上是目录」当成「存在冲突值键」。二者只在 `delete` 之前等价——`delete` 只删值不删父目录，此后层级载体上残留的**空目录**在扁平 KV 上根本不存在。修法引入删目录动作后，最大风险变成数据丢失，故 `removeEmptyDirTree` **只调 `readdir`/`rmdir`，绝不 `unlink`、绝不 `fs.rm({recursive})`**：判据即使写错，内核也会用 `ENOTEMPTY` 挡住。**教训**：当修复需要引入破坏性原语时，先让原语本身在物理上做不到最坏的事，再谈判据对不对。
- **任务 4.2（2026-07-21）**：errno 兜底在层级 FS 上**原理上不够**——① 勘误⑧ 要求错误指名冲突键，errno 给不出；② 空分组必须写入**成功**，而它与非空分组在 errno 层面都是 `EISDIR`。故必须主动探测；errno 只能作并发窗口的兜底，且**兜底后必须重新探测再分类**，否则真实 IO 故障会被误报成「宿主键写错了」。契约初稿「层级 FS 免费」已据此改正。
- **任务 4.2（2026-07-21）· 方法**：「这条断言有判别力吗」的最终答案是**变异测试**——把实现临时改回错误写法，捕获红，再还原。本任务用它把一句论证变成了证据（`expected 'key' to be 'io'`）。成本约两分钟，比争论便宜。
- **任务 4.3（2026-07-21）· ★方法论上的关键一课**：一条测试**声称**自己是守卫，不等于它是。本任务有一条用例自述「将来 guard 被摘掉时留下可读判据」，复核者用变异体实测：**guard 完全摘掉（M0）时它仍是绿的**，M6/M7 下亦全绿——零杀伤力，且与 4.2 既有用例是同入口同形状的纯重复。**一条伪装成守卫的恒真断言比普通恒真断言更坏**，因为它让人以为那个方向有人看着。判据只有一个：拿变异体杀它，杀不动就是没用。
- **任务 4.3（2026-07-21）**：`NaN` 上限曾**静默失效**——`size > NaN` 恒为 `false`，任意大的值都写得进去。env 路径对同样的非法值是 fail-fast 的，两条入口口径分裂就是缺陷。校验落点选在 `createLocalWorkspaceNamespace`（上限进入比较的**唯一咽喉**），变异体 M7（把 guard 上移到装配层）证实那样会漏掉直接调用工厂的路径。
- **任务 4.3（2026-07-21）**：实施者给落点的**理由**曾有一半是错的（称一致性套件走命名空间工厂，实际 `ConformanceTarget.workspace` 是 `Workspace` 类型，走的是装配入口）。结论对不代表论证对——**注释里断言一个假事实比不写更糟**，未来读者会依赖它做判断。
- **任务 4.3（2026-07-21）**：`WorkspaceConfigError.envName` → `source`。改名的**期限**是该类经 barrel 成为对外公开面之前（6.1/6.2）；一旦 pi-clouds 跨仓消费，成本就从 3 行变成跨仓破坏性变更。**教训**：共享类型上的语义污染有到期日，越界与否要按到期日算，不能只按当前任务边界算。
- **design.md 曾自相矛盾**：`LocalWorkspace` 的 Responsibilities 要求它承担根解析，接口块却把两根定为**必填**——必填则该职责无处可落。已按「职责 + 契约 §3.5」改为可选并补 `env`/`cwd` 注入项。
- **任务 4.4（2026-07-21）· ★★覆盖纪律（本 spec 最重要的一条）**：`delete`/`exists` 把键校验写在 `try` 内，`WorkspaceKeyError` 被 errno 兜底重包成 `WorkspaceIoError`（判别码 `key → io`），**经 4.1/4.2/4.3 三轮独立复核全绿而未被发现**。复核者用变异体实证：还原该缺陷后 `local-workspace.test.ts`(36) + `local-workspace-assembly.test.ts`(23) **依旧全绿**，只有一致性套件红 28 条。**教训**：单测覆盖「校验函数本体」+「方法正常路径」= 端口层**错误判别码零覆盖**。凡契约按 `code` 分流的方法，**每个方法**都必须有一条「非法输入 → 期望 code」的用例；靠校验函数的单测替代它，是覆盖率上的幻觉。
- **任务 4.4（2026-07-21）**：这是「套件即契约的可执行部分」第一次兑现——套件照契约写、实现照契约写，二者**首次对接**就抓出三轮复核都没抓到的缺陷（32 failed/62）。**两份独立的契约解读相互检验，比同一份解读被检查三遍更有效。**
- **任务 4.4（2026-07-21）· 声明不是豁免**：`ConformanceTarget.caseSensitiveKeys` 声明 `false` 后，用例**不跳过**而是**反向断言**（两键确实互为别名、后写者胜出、列举只回一条），谎报同样红。豁免式的声明等于让实现自证清白——凡「实现可声明某条不适用」的机制，都必须设计成谎报会被抓出，否则它就是一个后门。已写入契约勘误⑪。
