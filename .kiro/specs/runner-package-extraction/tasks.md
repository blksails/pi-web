# Implementation Plan: runner-package-extraction

> ★ **本计划的次序有一处不可调换**：守卫（任务 2）必须**先于**搬迁（任务 3）扩展到三包。
> 源码直连 + 跨包导入使「搬错包」在类型层完全可能通过 —— 照样能编译、能跑测试，
> 只有守卫能抓住。守卫后置等于搬迁期间无人看管。

---

- [ ] 1. 基础：新包骨架与解析登记

- [x] 1.1 建立 runner 包的清单与依赖面
  - 新建包目录与 `package.json`：包名 `@blksails/pi-web-runner`，ESM，源码直连（无构建步骤）
  - 导出面三条：主入口、**引导脚本子路径**、以及与内核同形的通配子路径
  - 运行时依赖仅：内核 / 协议 / 日志 / 工具包 + 模块加载器；agent 运行时 SDK 声明为 **peer 且不标可选**
  - ⚠ 把 SDK 放进 `devDependencies` 会直接触发 peer-only 守卫 —— 那是设计意图，不是障碍
  - 可观测完成态：`pnpm install` 后 `packages/runner` 出现在工作区包列表中，且其 `dependencies` 不含 `e2b` / `pg` / `ws` / MCP SDK 四者中任何一个
  - _Requirements: 1.2, 1.3, 1.5_

- [x] 1.2 建立新包的类型检查与分档测试基建
  - 以内核包的同名文件为样板建 `tsconfig.json`、`vitest.config.ts`、分档 workspace 配置与分档编排脚本
  - ★ **并补上新包 `package.json` 的 `scripts` 块**（`typecheck` / `test` / `test:fast` / `test:e2e`）——
    任务 1.1 有意未建（当时编排脚本还不存在）。缺它时根 `pnpm -r run typecheck` 会**跳过**新包：
    新包悄悄不在根类型检查里，而根类型检查照样 exit 0
  - `tsconfig` 的 `rootDir` 取上一层 —— 跨包引用内核的 `.ts` 否则触发 TS6059
  - ★ `vitest.config.ts` **必须存在**，哪怕看起来多余：包内缺它会静默继承仓库根配置，症状是「No test files found」
  - ★ 分档 workspace 里的 setup 共享件**跨包引用内核包的对应文件**（兼容层已是这么做的）；且根级 setupFiles 在存在 workspace 文件时会被**完全忽略**，必须写进每个 project
  - ★ `--no-isolate`（fast）与 `--no-file-parallelism`（it/e2e）**只能走 CLI 标志** —— 写在 project 级会被 vitest 2.1.9 静默忽略
  - fast 档沿用「不给 `--passWithNoTests`」的约定 —— 档位为空必须是一次响亮的失败
  - ⚠ 由此推出：**测试搬入（任务 3.3）之前，新包的 fast 档必然为空、按设计就该失败**。这不是缺陷，故本任务的完成判据**不含** fast 档转绿 —— 它在 3.3 完成后才应转绿
  - 可观测完成态：`pnpm --filter @blksails/pi-web-runner typecheck` exit 0；分档编排脚本能被调起并**明确报出「fast 档为空」**，而非静默通过
  - _Requirements: 6.4_

- [x] 1.3 在全仓解析配置中登记新包
  - 仓库根测试配置加入新包的别名条目；★ `.js`→`.ts` 的正则条目**必须排在具名子路径之前**（字符串 find 是前缀匹配，否则拼出四不像路径）
  - 确认服务端构建的别名表**不加**新包 —— 与内核同理，走通配子路径导出由解析器原生展开
  - 仓库根的 fast / e2e 测试脚本纳入新包
  - ★ **仓库根 `pnpm -r run typecheck` 本来就是红的** —— `desktop` 包的 typecheck 是 `cargo check`，在本机挂在 Rust 构建上，与本 spec 无关。故判据须**排除 desktop** 取范围，或改用「加入新包前后同一范围的结果不变」的对照式判据
  - 可观测完成态：仓库根 `pnpm test:app` 与 `tsc -p tsconfig.json --noEmit` 均不因新包而失败
  - _Requirements: 6.4, 1.5_

- [ ] 2. 守卫先行：三包全覆盖（★ 必须早于任务 3）

- [x] 2.1 把新包纳入被守卫扫描的包根名册
  - 包根名册增加第三项；沿用「每个包根必须至少贡献一个文件」的非空校验
  - ★ 自证：临时把新包根的路径改错 → 守卫必须报红而非报绿。**没装上的守卫报出的绿，和真的没有违规长得一模一样**
  - 可观测完成态：名册相关用例全绿，且上述自证实验确实报红
  - _Requirements: 5.1, 5.2, 5.3_

- [x] 2.2 (P) 把层归属⟹物理归位断言推广为映射表驱动
  - 现断言硬编码只查内核包，新包成立后对 runner 层**恒真** —— 那正是要根除的失效模式
  - 改为「层 → 包根」映射驱动，双向断言对表内每一项执行；每个包根做非空校验
  - 保住防重言性质：判据两端仍是两个独立事实源（名册是人写的层声明，磁盘是实际归位）
  - 可观测完成态：故意把一个 runner 层模块的名册归属改成内核层 → 断言报红并指出该模块
  - _Requirements: 5.5_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/module-roster.test.ts`_

- [x] 2.3 (P) 把新包纳入跨包通配解析与反向边检测
  - 通配包根集合纳入新包，使其深路径 specifier 能被解析回本仓模块
  - ★ 该集合是**按「包是否声明通配子路径导出」自动筛出来的**：新包若漏声明通配，它的跨包深路径边会被当成外部依赖直接跳过 —— **守卫永远绿**。故本任务须显式断言新包在该集合内
  - 反向边检测覆盖新包：新包对兼容层的任何导入都应被判为反向
  - 可观测完成态：临时在新包里加一条指向兼容层的导入 → 守卫报红并指出源与目标
  - _Requirements: 5.4, 5.1_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/dependency-guard.test.ts`_

- [x] 2.4 (P) 把新包纳入包依赖审计
  - 禁用依赖清单与 peer-only 清单对新包同样执行，违规须报出**具体依赖名与所在字段**
  - ★ 现有审计用例**硬编码只查内核包**（按包根短名筛选），必须解除该硬编码 —— 否则新包的依赖面根本没被审，而用例照样全绿
  - 可观测完成态：临时把数据库驱动加进新包 `dependencies` → 审计报红并同时指出依赖名与字段名
  - _Requirements: 1.4_
  - _Depends: 2.1_
  - _Boundary: C5 守卫扩展 — `packages/core/test/tiering/package-deps.test.ts`_

- [ ] 3. 搬迁：runner 实现、引导脚本与测试

- [x] 3.1 迁移 runner 实现的全部模块
  - 28 个实现文件整体迁入新包，目录名保持不变；模型源注册契约随该模块整体移动
  - ★ **命名不是归属判据**：名字带 `RUNNER_` 前缀但实现属 adapters 层的三个环境变量常量**不得**随包搬走 —— 搬走会让兼容层主入口少 3 个符号
  - 出向依赖已全部是包级 specifier，静态上行边为 0；仅两条动态上行边保持原样（已在豁免表登记，本任务不扩大）
  - 可观测完成态：新包内实现文件数与迁移前一致，且 `pnpm --filter @blksails/pi-web-runner typecheck` exit 0
  - _Requirements: 1.1, 2.3_
  - _Depends: 2.1, 2.2, 2.3, 2.4_

- [x] 3.2 迁移引导脚本并修正其内部路径
  - 引导脚本迁入新包根部；其内部指向 runner 实现的路径随之更新
  - 模块解析根（jiti 根）随之变为新包目录 —— 这正是内置扩展可解析性的前提（见任务 5）
  - 可观测完成态：以新包内的引导脚本直接启动一次 runner 子进程能进入就绪状态
  - _Requirements: 1.1_

- [x] 3.3 迁移 runner 的测试并保住分档归位
  - 单元档与集成档测试整体迁入新包；跨包测试固件的相对路径随之修正
  - ★ **文件名后缀逐字保留**：fast 档把子进程模块别名到抛错守卫（导入不报错、**调用才报错**），真实启动子进程的测试一旦丢了集成档后缀会在调用点炸
  - ★ 迁移脚本若按扩展名过滤，注意非 `.ts` 的子进程固件 —— 类型检查对这类运行时路径字符串**完全无感**（上一个 spec 因此漏搬 5 个固件）
  - 可观测完成态：新包各档的测试文件数与迁移前逐档相等，且真实启动子进程的测试全部落在允许启动子进程的档位
  - _Requirements: 6.3, 5.2_

- [x] 3.4 兼容层接线：契约不变地消费新包
  - 兼容层声明对新包的依赖；模型源装配处改用包级 specifier 引用注册契约（跨包后方向仍为正向）
  - 兼容层的分发文件清单去掉引导脚本（它已不在本包）
  - ★ 主入口**不得**顺手把 runner 的公共面并进来 —— 那个缺口是有意的，保持原样
  - 可观测完成态：主入口符号基准用例通过，符号集合与改动前**逐字相同**；6 条子路径导出一条不少
  - _Requirements: 2.1, 2.2, 2.4, 2.5_

- [ ] 4. 引导路径：改为包解析（★ 唯一逻辑变更）

- [x] 4.1 把引导路径改为三级解析并补上存在性检查
  - ★ 本任务**不是搬迁**，是有意的机制变更，故单列：现状第二级**无条件返回**且不做存在性检查，失败被延后到子进程启动时才以 ENOENT 现形，错误现场离根因很远
  - 第一级改用包解析（与工作目录无关）；第二级保留工作目录兜底但**补上存在性检查**；两级皆不成立则**抛错并列出所查过的全部位置**
  - 解析函数**留在兼容层**（层归属为装配层，且在主入口符号基准内），且**绝不导入新包的实现** —— 只做解析。导入会把 runner 与整套 agent SDK 拉进服务端产物，那正是主入口刻意排除 runner 的原因
  - 函数签名保持不变
  - 可观测完成态：三条分支各有单元用例；失败分支的错误消息**包含所查过的两个位置**
  - _Requirements: 3.1, 3.2, 3.3, 2.5_

- [x] 4.2 修正被实测证伪的注释并同步本仓路径常量
  - ★ 两处文件头声称构建器会把模块自身 URL 内联为构建机绝对路径 —— **实测为假**（产物里它是活的，共 7 处）。那是 webpack 时代的行为，迁到当前构建器后已不成立。留着会让后人据此否掉正确的方案
  - 修正时写上实测命令，使后人能复核而非只能相信
  - 沙箱镜像的引导脚本路径常量、命令行端到端测试的产物完整性清单，随包名同步更新
  - ★ 跨仓的镜像构建文件**只登记不改**（本 spec 范围外），并写明它消费的是**已发布的 npm 包** —— 即使本仓全绿也要等基础镜像重烘焙才现形
  - 可观测完成态：全仓搜索不到指向旧包引导脚本路径的常量；两处过时注释已改写并附实测命令
  - _Requirements: 3.6_

- [ ] 5. 内置扩展在新解析根下的可用性

- [x] 5.1 验证三个内置扩展在新包解析根下实际装载
  - 工具包必须是**新包**的运行时依赖 —— 各扩展入口函数用自身模块位置推算，解析根随引导脚本迁移而变
  - ★ 降级语义**不改**：解析不到时记警告后跳过。把它升级为抛错会让某形态缺代码时从「能力不可用」变成「会话失败」，属行为变更，明确在范围外
  - ★ 判据取「解析结果为 3 条入口」，**不取**「没有报错」—— 后者与「三个都解析不到但日志没人读」无法区分
  - 可观测完成态：装载断言返回 3 条真实存在的入口路径
  - _Requirements: 4.1, 4.2, 4.3_
  - _Depends: 3.2_

- [ ] 6. 验证与证据

- [x] 6.1 本仓通过面与类型检查不回退
  - 三个包的测试与仓库根测试面全量运行，**连跑两次**并比对
  - ★ 核对汇总行算术：通过数 + 跳过数是否等于总数。不相等意味着有 worker 静默崩溃 —— 那会被计成「0 failed」（本仓已被骗过一次）
  - 三包与仓库根的类型检查均 exit 0；★ 根 `pnpm -r` 需排除 `desktop`（其 `cargo check` 是既有红，与本 spec 无关，见 1.3 的说明）
  - 可观测完成态：两次运行的文件数与用例数**逐档一致**且不低于开工快照；实测输出（含耗时）留档
  - _Requirements: 6.1, 6.2_

- [ ] 6.2 部署态证据：换机复现与产物包解析
  - 构建完整产物；跑换机复现的端到端验证（藏起构建目录）
  - 在真实产物树里实测：从产物入口位置解析新包的引导脚本子路径，须解析到真实存在的文件
  - ★ **开发态运行与单元测试的通过不作为本项证据** —— 开发态下主路径与兜底路径恒等命中，它们的绿对引导路径正确性零信息量
  - 可观测完成态：换机端到端验证通过；产物树解析实测输出留档（含解析到的绝对路径）
  - _Requirements: 3.4, 3.5, 3.1_
  - _Depends: 4.1, 4.2_

- [ ] 6.3 交付证据矩阵与已知未验清单
  - 逐条需求对应到机械证据（命令 + 实测输出），而非「全绿」结论
  - 桌面形态与沙箱烘焙形态**显式列为已知未验**，写明风险与触发条件（后者需跨仓改动 + 基础镜像重烘焙才可验）
  - ★ 交付前走完成度闸门取新鲜证据 —— 断言不等于证据
  - 可观测完成态：证据矩阵覆盖全部 29 条验收标准，每行给出命令与实测输出摘要；未验项单独成节
  - _Requirements: 3.6, 6.5, 4.3_
  - _Depends: 6.1, 6.2_

## Implementation Notes

- 1.1：新包 `package.json` 刻意**不含** `scripts` 块 —— 脚本要指向任务 1.2 才建的分档编排文件，先写会指向不存在的路径。已把补 `scripts` 的要求写进 1.2，并记下其静默性质：`pnpm -r run typecheck` 跳过无该脚本的包，根类型检查照样 exit 0。
- 1.1：peer 版本取 `^0.80.3`（与兼容层现有依赖一致）而非内核用的 `*`。宿主仍可在范围内选版本，且记录了实测通过的范围。
- 1.2：`tsconfig.include` 除 `src`/`test` 外还纳入两个 vitest 配置文件 —— `src/` 在任务 3.1 前为空，不纳入别的文件 tsc 会因「无输入」报错。src 填充后这条无害且顺带检查了配置本身。
- ★ 1.2：**仓库根 `pnpm -r run typecheck` 是既有红**（`desktop` 的 `cargo check` 失败），与本 spec 无关。任何以「根类型检查 exit 0」为判据的地方都必须排除 `desktop`，否则会把既有红误记成本次改动的回归。已同步修正 1.3 与 6.1 的判据。
- ★ 1.2：新包真的进了根类型检查，由判别性实验证明 —— 在 `packages/runner/src/` 埋一个类型错误，根 typecheck 报 `packages/runner typecheck: src/__probe.ts(1,14): error TS2322` 并 exit 2；移除后回到 exit 0。只跑一次绿无法区分「检查了且没问题」与「压根没检查」。
- ★ 1.3：**vite 的字符串 alias 是朴素前缀匹配** —— `"@blksails/pi-web-X/"`（带尾斜杠）那条会把**裸** specifier 一起吃掉，解析成目录 `packages/X/src`，再由目录索引找 `index.ts`。因此两个包的**字符串裸名条都是死代码，从未被匹配到**（实测：把 core 的裸名条指向不存在的文件，裸 core 照样解析；把前缀条打断，裸 core 立刻失败）。core 蒙对是因为它恰好有 `src/index.ts`；runner 主入口在 `src/runner/index.ts`，于是裸名解析失败（目标文件存在也照样失败）。修法是加一条 **`$` 锚定的正则**裸名条排在前缀条之前 —— 不能改用字符串裸名条排前面，朴素前缀匹配会连深路径一起吞掉。
- ★ 1.3：根 `test:fast` / `test:e2e` 的串联把 runner 排在**末尾**。它在任务 3.3 前必然失败（fast 档为空），排在中间会短路掉后面的包 —— 实测过一次：`core && runner && server` 时 server 完全没跑到。排末尾后 core（122 文件）与 server（71 文件）在整个搬迁期间继续受检。
- ★ 1.3：根 `pnpm test:app` 有**既有崩溃**：`Tests 1019 passed | 2 skipped (1031)` —— 1019+2=1021≠1031，10 个用例没跑，worker `heap out of memory`。该文件自基线 `99d122a3` 起本分支从未碰过，属上游存量。验收时须核对汇总行算术，否则会被计成「0 failed」。
- ★ 2.1：「守卫先于搬迁」带来的张力（新包已成立但 `src`/`test` 还空着，非空校验会立刻报红）用**翻转判据**解决，而非放宽：包根可按维度声明 `pendingContributions`，语义是**必须恰好为 0 个文件**。于是任何时刻恰有一条约束生效 —— pending 时「必须为空」，非 pending 时「必须非空」，**没有一刻无人看管**。这个豁免只能通过让守卫变红来退场，不可能被遗忘。维度分开声明是必要的：实现（3.1）与测试（3.3）在不同任务里搬，单个布尔标记会让两者之间的测试维度凭空变红。
- 2.1：判别实验三条全部验过 —— 删 pending → 红；pending 且有文件 → 红并指名该删哪个键；包根路径写错 → 红。还原后 `1154 passed | 3 skipped (1157)`，算术自洽。
- ★ 2.2：层⟹物理断言改为 `LAYER_PLACEMENT` 映射表（`Record<Layer, …>`，新增一层漏表态即类型错误）。过渡期用 `stagedIn` 记录模块此刻实际所在的包根，但**只允许在目标包根仍标着 `srcModules` pending 时存在**且守卫当场核这一条 —— 与 2.1 同构：3.1 一落地、pending 被逼着删掉，`stagedIn` 随即过期报红，判据自动恢复严格。**没有永久豁免的余地**。
- ★ 2.3：加了显式断言「需要被深路径引用的包都声明了 src 通配子路径」。漏声明的后果**不是导入报错，而是守卫失明** —— 该包的深路径 specifier 会被当成外部依赖静默跳过，它所有跨层边从守卫视野里消失。另加一条防重言断言：包根短名拼错会让上面那条恒真。
- ★ 2.4：审计从硬编码 `r.name === "core"` 改为**每个包根各自声明规则**。兼容层合法地依赖 e2b/pg/ws/MCP SDK（adapters 未切出），故不能无差别套同一套禁令；而漏给新包根定规则必须响亮失败，不能默默按最宽松处理。
- ★ 2.2/2.3/2.4：三条判别实验由父层独立复跑（并发子代理的自报不足采信）——层归属改错 → `runner(core) 不在 core 包`；拿掉通配导出 → 「守卫失明」；pg 进 dependencies → `pg @ dependencies(数据库驱动)`（名与字段同现）。每次实验后均确认无残留。合并后 `1169 passed | 3 skipped (1172)`，算术自洽。
- ⚠ 教训：跑判别实验时把「改配置 → 跑全量 → 还原」串成一条命令跑了 4 遍，超时中断，导致 `packages/runner/package.json` 停在被改状态（`pg` 残留）。改为**逐个实验 + 只跑相关那一个测试文件**（`npx vitest run <file> --project fast`，约 1s）。破坏性实验必须单独一步、还原单独一步。
- ★ 3.1：两条动态上行边改写为**包级 specifier**（`@blksails/pi-web-server/host-assembly/*.js`），兼容层为此新增两条**具名子路径导出**来承接。新包**不声明**对兼容层的依赖 —— 那是反向的包依赖；这条边的语义本就是「运行期由宿主提供」。★ 关键验证：具名导出让守卫**仍能解析并看见**这条跨包边 —— 临时删掉 `ALLOWED_EDGES` 里的豁免，守卫立刻报 `runner(runner) → host-assembly(assembly)`。若换成守卫解析不到的形式，这条边会静默消失，豁免登记就形同虚设。
- ★ 3.1：`stagedIn` 已按守卫要求从映射表删除（搬迁落地即过期），但**机制本身保留**且用可注入的表继续测 —— 否则"过渡期判暂存包"这条逻辑将永远无人覆盖。`pendingContributions` 只剩 `testFiles`（测试到 3.3 才搬）。
- ★ 3.1：命名陷阱已避开 —— 三个 `RUNNER_` 前缀常量仍在 `ai-gateway/session-model-source.ts`（adapters 层）。权威判据是主入口符号基准用例通过，而非人工比对。
- ⚠ 3.1：我在派单里写的校验「根 `pnpm -r`（排除 desktop）typecheck exit 0」**在 3.3 前不可能成立** —— `packages/server/test/runner/*` 仍指向旧址，53 个 TS2307。这是我自己的判据错误，不是实施缺陷。该判据应推迟到 3.3 之后。
- ★ 3.2：核心交付是**真实起一次子进程**，父层独立复跑：以新包内的引导脚本启动 `examples/builtin-tools-agent`，6 秒后进程仍存活，stderr 走完 `runner boot → runtime built → attachment wiring → entering rpc mode`，stdout 发出真实 UI 帧（sandbox 状态 10 domains / schedule-prompts widget / mcp 状态）。判据取「进程存活且进入 rpc 模式」，不取「没报错」—— 一个卡在 MODULE_NOT_FOUND 立刻退出的进程同样什么都不打印。
- ★ 3.2：搬走后 `runnerBootstrapPath()` 的**两条路径都指向已删除的文件**（主路径与 cwd 回退都是 `packages/server/runner-bootstrap.mjs`）。这是预期的中间状态，也把本 spec 要治的病演示了一遍：第二级不做存在性检查就返回，调用方要到 spawn 时才拿到 ENOENT。4.1 修。
- ★ 3.3：79 个 `git mv`。逐档核对与基线一致 —— fast 27（基线 27）、it 20（基线 12 + 8 个 `*-subprocess.it`），合计 47。**4 个非 `.ts` 固件一并搬迁**（上个 spec 正是漏了这类，类型检查对运行时路径字符串完全无感）。
- ★ 3.3：`packages/server` 的 53 个 TS2307 清零，整仓（排除 desktop）typecheck exit 0 —— 3.1 以来首次恢复。
- ★ 3.3：`testFiles` 的 pending 已按守卫要求删除。至此 2.1/2.2 立的三个「只能通过变红来退场」的豁免（`srcModules` / `srcFiles` / `testFiles` + `stagedIn`）**全部如期退场，无一被遗忘在代码里**。
- 3.3：算术核对全部自洽 —— runner fast 198+1=199 / it 76+5=81（文件 19+1=20），core 1169+3=1172，server 525+5=530。根 `pnpm test:fast` 串联恢复 exit 0（1.3 记的「runner 档必然失败」的中间状态结束）。
- ★ 3.4 判断题（新增子路径导出是否违反 2.2）：**不违反，但须显式登记**。2.2 的原文是「保留其全部子路径导出，一个都不减少」—— 新增不是减少，字面合规。且这两条是 runner 两条动态上行边的**忠实跨包翻译**：原代码 `await import("../host-assembly/session-store.js")` 引的就是**具体文件而非 barrel**（避免把整个装配面拉进来），跨包后要保住同样的窄度就只能声明具名子路径。⚠ 但它确实**永久拓宽了兼容层的公开 API**（发包后即可被外部依赖），属 design 列的 Revalidation Trigger，不得当作零成本改动。兼容层导出面：原 6 条 → 8 条。
- ★ 3.4 发现 3.3 的一处漏网：`e2e/node/source-settings-endpoint.e2e.test.ts` 里指向 fixture 的**运行时路径字符串**未随 3.3 更新。与 4 个 `.mjs` 固件同一失效类别 —— 类型检查对这类字符串完全无感。父层随后全仓扫描 `packages/server/test/{runner,integration}` 确认无其它漏网。**这类漏网只能靠专门搜索发现，不会有任何测试或类型检查报错。**
- ★ 4.1（本 spec 唯一有意的逻辑变更）：三级解析落地。父层独立复跑的**决定性实验** —— 在 `cwd=/tmp`（第②级不可能命中）下调用，仍返回 `packages/runner/runner-bootstrap.mjs` 且 `existsSync` 为 true ⇒ **第①级包解析真在起作用，且与工作目录无关**。这正是整个 spec 要的性质：旧实现两条路径在 dev 态**恒等命中**，主路径断了也看不见；现在换个 cwd 就能观察到第①级是否成立。
- ★ 4.1 三条分支各自验过：① 正常返回真实路径；② 拆掉新包的 `./runner-bootstrap.mjs` 导出条目 → 降级到 cwd 兜底并命中；③ 导出条目拆掉 + cwd 换到 `/tmp` → **抛错且两个位置都列出**，各带确切原因（`ERR` 原文 / 具体路径）。实验后 `git diff` 确认 package.json 无残留。
- 4.1：新增 `resolveRunnerBootstrapPath` / `defaultResolutionDeps` / `RunnerBootstrapResolutionDeps` 三个 `@internal` 符号（供单测驱动分支 ②③，真实进程里包解析恒成功、无法稳定复现）。已核实**未泄进主入口**，313 符号基准用例通过。
- ★ 4.2：`build-server.mjs` 文件头改注释时**保住了结论**。那段论证「入口必须位于产物根」的**结论仍成立**（`bin/pi-web.mjs` 以 `dirname(serverJs)` 作 cwd，且 `resolvePiCliEntry()` 仍依赖第②级），错的只是**理由**。改注释最容易连正确结论一起删掉。
- ★ 4.2：跨仓账记在本仓 —— `bake-plan.ts` 的常量注释里登记了三步（pi-clouds 的 `Dockerfile.pi` 改 `npm i -g` 包名、新包发 npm、基础镜像重烘焙），并写明**本仓全绿不代表真机可用**：该路径指向的是基础镜像里已发布的 npm 包，不是本仓源码树。在三步完成前，沙箱烘焙形态属**已知未验**（接 R3.6）。
- 4.2：`bake-plan.test.ts` 的断言写死全串是刻意的 —— `AGENT_CMD` 是与基础镜像的**字节契约**，契约变了就该在那里响亮地红一次。故改测试是对的，不是迁就。
- 4.2：剩余 7 处**纯交叉引用式**注释漂移（`file-sink.ts`×2、`assemble-spawn.ts`、`surface-registry.ts`、`pi-handler.ts`×2、`attachment-tool-bridge-stub.mjs`）留给文档轮次 —— 它们是「见 X」式导航，不改变决策。但父层额外修了 1 处：`packages/runner/src/runner/runner.ts:12` 文件头里的**可执行命令**仍写着旧路径，照着敲会失败 —— 那是指令不是叙述，且就在本 spec 搬过来的文件里。
- ★★ 5.1 最重要的发现：**「默认清单解析出 3 条」这条断言在 monorepo 里恒真，守不住真正会炸的那个点**。父层做判别实验时把 `@blksails/pi-web-tool-kit` 从新包 `dependencies` 摘掉、连 `packages/runner/node_modules` 下的链接也一并删除 —— 解析**仍返回 3 条**，因为 Node 会向上走到仓库根的 `node_modules/@blksails/pi-web-tool-kit`。也就是说那条断言通过了，但**通过的理由是错的**。而真实安装树（沙箱镜像 / standalone，只装本包、没有仓库根那层）恰恰会炸，表现是三个内置扩展**静默不可用**——该失效模式历史上已发生过一次。
- ★ 5.1 因此补了一条**静态断言**：tool-kit 必须声明在新包自己的 `dependencies` 里。声明是静态事实，不受 monorepo 提升干扰，摘掉即红（已验：报红并给出「沙箱与 standalone 只装本包，届时扩展会静默不可用」的解释）。两条断言分工：解析断言证明「本仓语境下能装载」（R4.3），声明断言证明「换到只装本包的安装树也能装载」（R4.1 的真实风险面）。
- 5.1：降级语义未改（解析不到 → `log.warn` → 跳过），符合 design C4 划定的范围外。产品代码仅订正一处注释（解析根的描述从「server 包目录」改为「引导脚本所在的包目录 → 现在是 runner 包」）。

### 6.1 实测证据（连跑两次，逐档一致）

| 包 | 档 | 文件 | 用例 | 算术 | 耗时 |
|---|---|---|---|---|---|
| core | fast | 121+2=123 | 1169+3=1172 | ✓ | 1.60s |
| core | mock | 3 | 9 | ✓ | 1.20s |
| core | it | 48 | 474 | ✓ | 16.92s |
| server | fast | 44+1=45 | 533+5=538 | ✓ | 1.39s |
| server | mock | 2 | 22 | ✓ | 0.52s |
| server | it | 16 | 103+1=104 | ✓ | 17.06s |
| runner | fast | 27 | 200+1=201 | ✓ | 2.32s |
| runner | it | 19+1=20 | 76+5=81 | ✓ | 114.53s |
| 根 app | — | 104+1=105≠**106** | 1019+2=1021≠**1031** | ★既有缺口 | ~70s |

类型检查：core / server / runner 各自 exit 0；根 `pnpm -r`（排除 desktop）exit 0；根 `tsc -p tsconfig.json` exit 0。

**与开工快照的比对（口径已对齐）**：基线 `e76ec162` 磁盘 285 个测试文件（含 3 个 e2e 档）⇒ `pnpm test` 执行 282；当前 HEAD 磁盘 287（含 3 个 e2e）⇒ 执行 284。两个口径各 +2，**无文件丢失**；用例 2564 → 2601（+37）。
⚠ brief 里「285 文件」是**磁盘总数**，逐档相加得的 284 是**执行数** —— 分母不同，不是回退。

- ★★ 6.1 **抓到一个真回归（本 spec 引入，非既有红）**：`test/sandbox-image-build.integration.test.ts:255` 是 `AGENT_CMD` 字节契约的**第二处**逐行断言，任务 4.2 改常量时只更新了 `packages/server/test/sandbox-image/bake-plan.test.ts`，漏了仓库根这处。隐蔽点在于 4.2 自己跑的 `server test:fast` / `core test:fast` / `typecheck` **全绿**，只有跑到根 app 档才现形。
- ★ 6.1 区分「新失败」与「既有红」靠的是与**记录在案的基线形态逐字比对**：既有 heap OOM 的表现是 `104 passed | 1 skipped (106)`；出问题时是 `1 failed | 103 passed | 1 skipped (106)` —— 总数缺口都是 1，很容易连新增的 `1 failed` 一起当老毛病放过。修复后回到基线形态逐字相同。已在两处断言各加注释说明彼此存在。
- 6.1 既有红复核（形态未变，非本次引入）：① `desktop` 的 `typecheck` 是 `cargo check`，挂在 Rust 构建；② 根 app 档 worker heap OOM 吞掉 10 个用例（`chat-app-logs-wiring.test.tsx`，本分支自基线起从未碰过）。
