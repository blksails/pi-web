# Implementation Plan

- [x] 1. 固化工具排贡献扩展点(宿主中立):协议槽定义与宿主透传
  - 协议插槽枚举新增工具排贡献点(additive,向后兼容);槽区域组件支持可选的共享状态透传,并输出 `data-pi-ext-prompt-toolbar` 容器
  - 工具排装配在内核控件之后、发送键之前渲染该槽;source 未声明时返回 null,不产生任何容器或占位
  - 观察完成态:protocol 与 ui 包 typecheck 通过;未声明贡献的 source 输入区 DOM 与改动前一致;`grep -r "aigc" packages/ui/src/chat packages/ui/src/web-ext packages/protocol/src` 零命中
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 7.3_

- [x] 2. 图像工具偏好级(tool-kit aigc)
- [x] 2.1 (P) 模型/尺寸清单下发与生成路由导出
  - 图像生成工具导出其路由清单(与图像编辑既有导出同款形态)
  - aigc 扩展装配期把「生成∪编辑」模型并集与四档尺寸写入会话共享状态清单键;seam 缺失时静默 no-op
  - 观察完成态:装配后共享状态中模型清单为两工具支持模型的并集、尺寸清单为四档(单测断言)
  - _Requirements: 2.2, 3.1_
  - _Boundary: aigcExtension 清单下发_
- [x] 2.2 (P) 参数决定链插入偏好一级与追问写回
  - 编排器依赖注入新增共享状态读取接缝(默认取会话 seam,测试可替换)
  - 参数合并阶段:白名单 {model, size} 内的参数缺省时读会话偏好;调用方显式给出的参数始终优先
  - 偏好命中的参数不再进入交互式追问;交互追问(有 UI 分支)中用户对白名单参数的选择写回会话偏好;白名单外参数不写回
  - seam 不可用时全部行为与改动前完全一致(默认模型/照常追问),不报错
  - 观察完成态:注入的 fake 状态含模型偏好且调用未显式指定时,工具产物采用偏好模型且无追问发生
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 5.1_
  - _Boundary: runImageTool 偏好级_
- [x] 2.3 偏好级单元测试
  - 用例覆盖:偏好采用/显式参数覆盖偏好/尺寸同前两项/seam 降级行为不变/追问选择写回/白名单外不写回/清单下发内容
  - 观察完成态:tool-kit 测试全绿且包含上述新用例
  - _Requirements: 2.2, 3.1, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 5.1_

- [x] 3. AIGC 快捷设置组件(ui · aigc 领域)
- [x] 3.1 (P) 快捷设置组件与导出
  - 组件经 props 注入共享状态接入;接入缺失时返回 null(不呈现、不报错)
  - 模型/尺寸两个紧凑选择器(匹配工具排既有控件风格);清单读共享状态清单键,未就绪时回退内置常量(与清单下发值一致)
  - 当前值订阅回显(外部写回后无需刷新自动更新);无选择且无历史记忆时显示"默认"占位
  - 变更时写会话偏好键并同步写浏览器本地记忆;挂载时若会话偏好为空且本地记忆存在则回填会话偏好(seed)
  - 输出 `data-aigc-quick-settings` / `data-aigc-model-select` / `data-aigc-size-select` 锚点;从 ui 包公开导出
  - 观察完成态:fake 状态下组件渲染两个选择器,选择后 fake 收到偏好写入且本地记忆同步更新
  - _Requirements: 2.1, 2.3, 2.4, 3.1, 3.2, 3.3, 5.2, 6.1, 6.2, 6.3, 7.2_
  - _Boundary: AigcQuickSettings_
- [x] 3.2 组件单元测试
  - 用例覆盖:无接入返回 null/清单来自共享状态与常量回退/选择写偏好+本地记忆/订阅推送回显更新/挂载 seed 回填/默认态占位
  - 观察完成态:ui 测试全绿且包含上述新用例
  - _Requirements: 2.1, 2.3, 2.4, 3.1, 3.2, 3.3, 5.2, 6.1, 6.2, 7.2_

- [x] 4. 集成接线:source 声明、app 层回归守卫与 e2e 支撑
  - aigc-canvas-agent 的 web 声明在工具排贡献点挂载快捷设置组件
  - chat-app 两个测试的 ui mock 补充新组件导出(防合并期同型事故复发)
  - 离线 stub agent 增加一条指令:派发模型偏好的状态帧(模拟"追问写回",供 e2e 回显守卫)
  - 观察完成态:test:app 全绿;stub 会话选 aigc-canvas-agent 后工具排出现 `data-aigc-quick-settings`
  - _Requirements: 2.1, 7.1_
  - _Depends: 3.1_

- [x] 5. 端到端验证
- [x] 5.1 浏览器 e2e
  - 场景一:选 aigc-canvas-agent → 快捷设置出现在工具排内且 DOM 顺序位于发送键之前
  - 场景二:选择模型 → 回显更新;刷新页面后选择仍在(本地记忆 + 会话状态回放)
  - 场景三:stub 指令派发偏好帧 → 选择器回显自动变化(追问写回回归守卫)
  - 场景四:hello-agent 会话零渲染快捷设置,输入区照常可用
  - 观察完成态:隔离 build(NEXT_DIST_DIR=.next-e2e)+ external server(fs:3100)下四场景全绿
  - _Requirements: 1.1, 1.2, 2.1, 2.3, 5.2, 6.1, 7.1_
- [x] 5.2 全量回归与中立复核
  - typecheck、workspace 全量测试、test:app 全绿;宿主中立 grep 复核零命中
  - 观察完成态:上述命令全部退出码 0
  - _Requirements: 7.3_
