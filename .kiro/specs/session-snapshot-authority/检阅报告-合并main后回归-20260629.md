# 检阅报告 · session-snapshot-authority 合并 main 后回归

> 全部 8 个示例源 + 核心快照回归**逐项浏览器实测通过**。

## 元信息

| 项 | 值 |
|---|---|
| 日期 | 2026-06-29 |
| 分支 | `feat/session-snapshot-authority` |
| HEAD | `1d40886` Merge main into feat/session-snapshot-authority |
| Worktree | `/Users/hysios/Projects/BlackSail/agents/pi-web-snapshot`(隔离实验 worktree) |
| dev server | `localhost:3010`(PORT=3010,与主 worktree 3000 并存;`Ready in 2.1s`) |
| 模型 | `~/.pi/agent/settings.json` 默认 `dashscope/qwen3.7-max`;实测 LLM 端到端可用 |
| 检阅依据 | `docs/webext-检阅-清单.md` |
| 目的 | 验证本分支(前端纯投影 + 渲染器注册单一真相源 + 权威快照)**合并 main 三组改动后**(移除 ctx.ui.custom 桥 + auto-session-title + AIGC 计时)核心 UI 与扩展系统无回归 |

## 一、逐源验收(全部通过)

| # | 源 | 结果 | 关键证据 |
|---|---|---|---|
| 1 | webext-layout-agent | ✅ | header 三区、panelRight、`asideWidth=70%`(初始 3:7)、比例切换器 3:7 pressed、extCount=3 |
| 2 | webext-slots-agent | ✅ | `extCount=15`(12 RESERVED + header/footer/panel-right)、background 走 fixture 不计入 |
| 3 | webext-background-agent | ✅ | 空屏态 saturate(0.72)/blob 0.3/glow 0;交互态 saturate(1.15)/blob 0.62/glow 1,两态全对 |
| 4 | webext-renderer-agent | ✅ | echo 富卡片命中 `EchoToolRenderer`,input/output=`hello pi-web`,`[data-pi-tool]=0`(注册表覆盖) |
| 5 | webext-artifact-agent | ✅ | 无 `.env.local`→无 base-url→`artifactIframe=0`(OFF 侧正确门控) |
| 6 | webext-declarative-agent | ✅ | primary=`262 83% 58%`(紫)、宽布局(max-w-5xl)、自定义空态、标题「Declarative · pi-web」、extCount=1、panelRight=false |
| 7 | webext-contrib-agent | ✅ | ui-rpc 斜杠贡献 `/deploy`+`/rollback`(其它源均无) |
| 8 | aigc-agent | ✅ | image_generation 工具运行 + 计时器(`1:01 Completed`)+ **select 对话框**选模型 + 缺密钥优雅降级(模型×环境变量表 + 解决方法),LLM 智能重试备选模型,会话不中断 |

### 源4 / 源8 的重点意义
- **源4** 验证了本分支「渲染器注册单一真相源(PART_KINDS)」在合并后**完整生效**:自定义工具渲染器正确覆盖默认卡片。这是合并最该担心的回归点 —— 无问题。
- **源8** 的 **select 对话框**(选生成模型)正是「dialog 子协议」—— 即 main 移除 `ctx.ui.custom` 桥后,富/双向交互的**标准替代机制**。它在合并后正常工作,反向印证 custom 桥删除**未削弱**交互能力。

## 二、核心快照回归(T11,全部通过)

- **busy/turn 快照归约**:发消息回合中 `data-pi-busy="true"`、发送键变「停止生成」;回合结束(agent_end)`busy=false`、发送键复位 —— **不卡死**。这是本分支修的核心 bug(此前「busy 永久卡 true」),实测正常。
- **auto-session-title**(合并自 main):aigc 会话 agent_end 后,持久化会话名经 `appendSessionInfo` 自动设为首条用户消息「生成一张赛博朋克城市夜景」(经 `GET /api/sessions?scope=cwd&sessionId=…` 证实)。auto-title 入口 `autoTitleEntryPath()` 正确解析。

## 三、附带观察(非阻塞,建议跟进)

1. **会话历史侧栏显示「暂无会话」**,但同一会话经 API 按 `sessionId` 查询能拿到带正确 `name` 的记录 —— 疑似侧栏 scope/刷新问题(侧栏默认 scope 与会话创建 cwd 不一致,或列表未实时刷新),**与本分支改动无直接关联**,但值得单独确认。
2. **底部 stats(Messages/Tools/Tokens)在 aigc 一轮后仍为 0** —— 与既有「stats 无 SSE 帧、须 `getStats()` 拉取」一致,非回归。
3. dev 日志偶见 `module.createRequire failed parsing argument` 两行 —— 未观察到对功能的影响,建议留意。

## 四、检阅方法备忘(供复验沿用)

- **chrome `fill`/`type_text` 写不进 pi-web 自定义聊天输入框**(自实现受控浮层,`taValue` 不更新、发送键不启用)。**可靠绕过**:`evaluate_script` 内用 `HTMLTextAreaElement.prototype` native value setter + 派发 `input` 事件注入文本 → 发送键启用 → 用**真实 chrome `click`(uid)** 点发送(注意:在 `evaluate` 内 `button.click()` 不一定触发 React submit,须用工具层 click)。
- picker 的普通 input 用 `fill` 正常。

## 五、结论

**本分支合并 main 后,8 个示例源 + 核心快照回归全部通过,未发现回归。** 关键点:
- 渲染器注册单一真相源(源4)合并后完整生效;
- custom 桥删除未影响交互(源8 dialog 子协议正常);
- busy/turn 快照不卡、auto-title 正常;
- 合并的 AIGC 工具计时器、降级路径均工作正常。

> 仅三处附带观察(侧栏 scope/刷新、stats 拉取、createRequire 告警)建议后续单独确认,均不阻塞。
