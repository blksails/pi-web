node_modules/
dist/
.next/
.next-e2e/
.next-stub/
.next-test/
.next-cli/
.next-debug/
.next-*/
coverage/
*.log
.DS_Store
.env.local
test-results/
playwright-report/
tsconfig.tsbuildinfo
next-env.d.ts
.kiro/*.png

# webext 视觉验收证据(大量截图,本地保留,不入库)
.kiro/specs/agent-web-extension-visual-acceptance/evidence/
# pi-web-cli e2e 证据截图(本地保留,不入库)
.kiro/specs/pi-web-cli/evidence/

.claude/*
!.claude/skills/

# Storybook build output
storybook-static

/.pi/
# pi 调度运行时状态(任意 agent source 的 .pi/ 下,运行时生成,不入库)
**/.pi/schedule-prompts.json
# pi-team 主控台状态(本地)
.pi-team/

# 个人照片 e2e fixture(隐私,不入库;本地放置后跑 aigc-image-edit e2e)
e2e/fixtures/iphone-mpf.jpg

# vite-spa-migration 产物
/dist/

# shared-runtime-payload 生成物(载荷 + 解包器)
/payload/

# pnpm 本地 store
.pnpm-store/

# 本地工具产物(代码索引/编辑器),不入库
.codegraph/
.cursor/

# 独立业务仓；pi-web 只承载通用 panes 地基。
/examples/aigc-agent/

# 内置 pane 的 guest 文档产物(spec host-builtin-panes)。由 scripts/build-builtin-panes.ts
# 生成,类型侧有 panes/generated.d.ts 垫片 —— 刻意不入库,否则「本地绿」可能只是因为工作树里
# 躺着一份没人生成的产物(本仓已三次踩到)。
/panes/generated.ts

# 仓内示例 agent 的 pane 文档中间产物(spec cli-agent-build)。各 build.ts 在 finally 里已
# 自行删除,此处属防御性忽略(构建中途失败/被中断时不留脏文件)。类型侧同目录下的
# pane-documents.generated.d.ts 是手工维护的垫片,不受此规则影响(规则只匹配 .ts)。
examples/*/web/pane-documents.generated.ts
