/**
 * 薄转发:实现已随 `model-sources/` 搬入 @blksails/pi-web-adapters
 * (spec: adapters-package-extraction,任务 3.1)。导出面逐字不变。
 *
 * 与同目录 `model-options.ts` 同理(见其文件头对「为何必须经本目录」的说明)。
 */
export * from "@blksails/pi-web-adapters/model-sources/vision-model-options.js";
