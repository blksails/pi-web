{
  "name": "@blksails/pi-web-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "pi-web 桌面版 — Tauri 薄壳，复用 standalone 自包含产物",
  "scripts": {
    "sidecar": "node ../scripts/fetch-node-sidecar.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "typecheck": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0"
  }
}
