---
description: 以安全审计视角检视目标(注入/鉴权/密钥/越权/不安全依赖)
argument-hint: "[文件路径,默认当前 Git 改动]"
---
请以**安全审计**视角检视:${1:-当前 Git 改动(先 `git diff` 再逐文件检视)}。

重点关注:
- 注入(SQL / 命令 / 路径遍历)与输入校验缺失
- 鉴权 / 越权与会话处理
- 密钥、令牌、凭证的硬编码或日志泄露
- 不安全的反序列化、危险 API、过期或含漏洞的依赖

对每处问题调用 `code_review` 工具以富卡呈现,按风险高→低排序,并给出可执行的修复建议。
