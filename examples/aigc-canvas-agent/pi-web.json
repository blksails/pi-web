{
  "$schema": "https://pi-web.dev/schema/pi-web.json",
  "id": "e2e/aigc-canvas-agent",
  "version": "1.1.0",
  "kind": "agent",
  "displayName": "AIGC Canvas 画廊",
  "description": "画廊 = attachment 物化视图 + 二创工作台(domain=canvas 的 AAS 实例),隔离 Pane 形态。入口 index.ts 由约定探测得出;pane-meta/panes-modules/routes 经 files 白名单入包;webext 产物走 .pi/web/dist 约定探测(需先 pnpm build:webext-examples)。1.1.0 = slots → panes 迁移(isolated-panes Wave 5);1.0.0–1.0.3 为更早的手工发布,已占用。",
  "files": ["pane-meta.ts", "panes-modules.ts", "routes/**/*.ts"]
}
